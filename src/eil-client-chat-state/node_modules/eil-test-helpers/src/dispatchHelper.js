// @flow

export default function dispatchHelper(input: Object | (dispatch: Dispatch<*>, getState: () => Object) => Object, dispatch: Dispatch<*>, getState: () => Object) {
  if (typeof input == 'function') {
    return input(dispatch, getState);
  } else {
    return input;
  }
}
