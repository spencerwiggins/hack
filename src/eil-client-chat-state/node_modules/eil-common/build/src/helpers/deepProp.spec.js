'use strict';

var _chai = require('chai');

var _chai2 = _interopRequireDefault(_chai);

var _deepProp = require('./deepProp');

var _deepProp2 = _interopRequireDefault(_deepProp);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var expect = _chai2.default.expect;

describe('deepProps', function () {
  describe('keys passed as an array of strings', function () {
    it('should return null if key doesn\'t exist', function () {
      var intent = {
        parameters: {
          otherUser: {
            name: 'Joe'
          }
        }
      };
      var result = (0, _deepProp2.default)(['parameters', 'otherUser', 'lastName'])(intent);
      expect(result).to.be.null;
    });

    it('should return null if obj is empty', function () {
      var result = (0, _deepProp2.default)(['parameters', 'otherUser', 'lastName'])({});
      expect(result).to.be.null;
    });

    it('should return value if the exists', function () {
      var intent = {
        parameters: {
          otherUser: {
            name: 'Joe'
          }
        }
      };
      var result = (0, _deepProp2.default)(['parameters', 'otherUser', 'name'])(intent);
      expect(result).to.deep.equal(intent.parameters.otherUser.name);
    });
  });

  describe('keys passed as a string', function () {
    it('should return null if key doesn\'t exist', function () {
      var intent = {
        parameters: {
          otherUser: {
            name: 'Joe'
          }
        }
      };
      var result = (0, _deepProp2.default)('parameters.otherUser.lastName')(intent);
      expect(result).to.be.null;
    });

    it('should return value if key the exists', function () {
      var intent = {
        parameters: {
          otherUser: {
            name: 'Joe'
          }
        }
      };
      var result = (0, _deepProp2.default)('parameters.otherUser.name')(intent);
      expect(result).to.deep.equal(intent.parameters.otherUser.name);
    });

    it('should return null if obj is empty', function () {
      var result = (0, _deepProp2.default)('parameters.otherUser.lastName')({});
      expect(result).to.be.null;
    });
  });
});
//# sourceMappingURL=deepProp.spec.js.map
