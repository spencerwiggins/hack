'use strict';

var _chai = require('chai');

var _chai2 = _interopRequireDefault(_chai);

var _objectReplace = require('./objectReplace');

var _objectReplace2 = _interopRequireDefault(_objectReplace);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var expect = _chai2.default.expect;

describe('objectReplace', function () {

  it('should copy all properties like objectAssign does', function () {
    var source = {
      hello: 'world',
      hola: 'mundo',
      target: 2
    };

    var target = {
      target: 1
    };

    var expected = source;

    (0, _objectReplace2.default)(target, source);
    expect(target).to.deep.equal(expected);
  });

  it('should prune any properties that dont exist in the source', function () {
    var source = {
      hello: 'world',
      hola: 'mundo'
    };

    var target = {
      target: 1
    };

    var expected = source;

    (0, _objectReplace2.default)(target, source);

    expect(target).to.deep.equal(expected);
  });
});
//# sourceMappingURL=objectReplace.spec.js.map
