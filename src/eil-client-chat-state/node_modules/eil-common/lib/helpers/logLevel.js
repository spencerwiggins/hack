"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

exports.default = logLevel;
var defaultLevels = {
  production: "warn",
  development: "debug",
  test: "warn",
  default: "info"
};

/**
 * Return a logLevel for the given environment.
 * @param  {string} env          A string representing the enviroment to retrieve the log level for
 * @param  {Object} options
 *         * levels - a map of new or overridden environment -> logLevel
 *         * defaultLevel (info) -
 * @param  {[type]} defaultLevel Changes the default logLevel for any non-specified environments (default: info)
 * @return {string}              The log level for the environment
 */
function logLevel(env) {
  var options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
  var _options$levels = options.levels,
      levels = _options$levels === undefined ? {} : _options$levels,
      _options$defaultLevel = options.defaultLevel,
      defaultLevel = _options$defaultLevel === undefined ? defaultLevels.default : _options$defaultLevel;


  var _levels = _extends({}, defaultLevels, levels);

  return _levels[env] || defaultLevel;
}
//# sourceMappingURL=logLevel.js.map
