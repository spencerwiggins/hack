// @flow

const deepProp = (keyOrKeys: string|Array<string>) => (obj: Object): any => {
  const keys = Array.isArray(keyOrKeys) ? keyOrKeys : keyOrKeys.split('.');
  return keys.reduce((xs, x) => (xs && xs[x]) ? xs[x] : null, obj);
};

export default deepProp;
