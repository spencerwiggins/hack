//@flow
export default function objectReplace(target: Object, source: Object): Object {
  Object.assign(target, source);

  for (let prop in target) {
    if (!source.hasOwnProperty(prop)) {
      delete target[prop];
    }
  }

  return target;
}
