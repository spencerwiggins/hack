//@flow

const stackReducers = (...reducers: Array<(state: Object, action: Object) => Object>) => {
  return (state: Object, action: Object) => {
    let _state = state;

    for (let reducer of reducers) {
      _state = reducer(_state, action);
    }

    return _state;
  };
};

export default stackReducers;
