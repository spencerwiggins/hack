//@flow

export default function humanize(input: string): string {
  return input
    .replace(/([a-z])([A-Z])/g, (matches) => `${matches[0]} ${matches[1].toLowerCase()}`)
    .replace(/-|_/g, ' ');
}
