// @flow
import React from 'react';
import { Listing } from './';

const ListingContainer = function ListingContainer (props) {
  return (
    <Listing {...props} />
  );
};

export default ListingContainer;
